extends ./layout
block content
    include ./includes/_filters
    table
        include ./includes/_table_headers
        tbody
            if content === "loans"
                include ./includes/_loan_table
            if content === "books"
                include ./includes/_book_table
                include ./includes/_pagination
            if content === "patrons"
                include ./includes/_patron_table
