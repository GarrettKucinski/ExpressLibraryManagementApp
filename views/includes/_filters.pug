
- var item = `${content.charAt(0).toUpperCase()}${content.slice(1, content.length - 1)}`;

h1= title  
a.button(href=`/${content}/new`) Create New #{item} 
if content !== 'patrons'
    p Filters 
        a(href=`/${content}`) All | 
        a(href=`/${content}?filter=overdue`) Overdue | 
        a(href=`/${content}?filter=checked_out`) Checked Out
else
    p 
